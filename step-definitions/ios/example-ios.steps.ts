import { Given, Then } from "@wdio/cucumber-framework"

Given(/^I open the iOS application$/, function () {
  // Your code to open the iOS application
  console.log("I open the iOS application")
});

Then(/^I should see the iOS home screen$/, function () {
  // Your code to verify the iOS home screen
  console.log("I should see the iOS home screen")
});
